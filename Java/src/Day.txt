1. 메모리구조
method area: static이 붙은 메소드와 변수들이 올라가는 영역
프로그램 실행시 가장 먼저 로드되고 종료까지 남아있는다.
heap: new 연산자로 생성된 인스턴스들이 올라가는 영역
stack: 메소드가 실행될때 차지하는 영역. 지역변수와 전역변수가 저장된다

2.자동형변환
좁은타입에서 넓은타입으로 형변환 하는 경우엔 값손실이 없어서
표현범위가 더 넓은쪽으로 형변환됨
byte->short,char->int->long->float->double

3.연산자
ㅁ비교
문자열의 비교 : .equals() 메소드 사용 / 대소문자무시 equalsIgnoreCase()
'=='연산자를 문자열에 이용하면 객체의 주소값을 비교하게 되어 다른결과가 나타날수 있기때문..
ㅁ논리
or결합: 어느한쪽만 true 면 true 반환
and결합: 둘다 true여야 true를 결과로 얻음
ㅁ조건
조건식? 식1 : 식2
      true  false
4.Java
자바는 객체지향프로그래밍 언어로써
컴퓨터의 운영체제에 독립적입니다.
자바로 작성된 프로그램은 별도의 변환없이 자바 자체의 JVM을 통해서
운영체제에 상관없이 실행이 됩니다.

5.클래스(class)
자바에서 클래스(class)란 객체를 정의하는 틀 또는 설계도와 같은 의미로 사용됩니다.
자바에서는 이러한 설계도인 클래스를 가지고, 여러 객체를 생성하여 사용하게 됩니다.
클래스는 객체의 상태를 나타내는 필드(field)와 객체의 행동을 나타내는 메소드(method)로 구성됩니다.
즉, 필드(field)란 클래스에 포함된 변수(variable)를 의미합니다.
또한, 메소드(method)란 어떠한 특정 작업을 수행하기 위한 명령문의 집합이라 할 수 있습니다.

6.인스턴스(instance)
자바에서 클래스를 사용하기 위해서는 우선 해당 클래스 타입의 객체(object)를 선언해야 합니다.
이렇게 클래스로부터 객체를 선언하는 과정을 클래스의 인스턴스 화라고 합니다.
또한, 이렇게 선언된 해당 클래스 타입의 객체를 인스턴스(instance)라고 합니다.
즉, 인스턴스란 메모리에 할당된 객체를 의미합니다.
자바에서는 하나의 클래스로부터 여러 개의 인스턴스를 생성할 수 있습니다.
이렇게 생성된 인스턴스는 독립된 메모리 공간에 저장된 자신만의 필드를 가질 수 있습니다.
하지만 해당 클래스의 모든 메소드(method)는 해당 클래스에서 생성된 모든 인스턴스가 공유하게 됩니다.

7.변수란 데이터(data)를 저장할 수 있는 메모리 공간을 의미하며, 이렇게 저장된 값은 변경될 수 있습니다

8.오버플로우(overflow)란 해당 타입이 표현할 수 있는 최대 범위보다 큰 수를 저장할 때 발생하는 현상을 가리킵니다.

9.객체지향프로그래밍
프로그램의 모든 데이터를 객체로 보는것이 객체지향프로그래밍입니다.
객체를 상태(필드)와 동작(메소드)로 구체화하여 설계한것이 클래스입니다.
이 클래스를 new연산자를 사용해 객체화 하여 사물로서 사용합니다.

10.this()
같은클래스의 다른생성자를 부를때 씀

11.오버로딩
같은 이름의 메소드를 중복하여 정의하는것
1)메소드의 이름이 같아야 합니다.
2)메소드의 시그니처, 즉 매개변수의 개수 또는 타입이 달라야 합니다.

12.인스턴스 초기화블록
인스턴스가 생성될때마다 실행됨
{} <-중괄호만을 이용

13.클래스 초기화블록
클래스가 처음으로 메모리에 로딩될떄 초기화
static {}

14.상속
상속이란 기존의 클래스에 기능을 추가하거나 재정의하여 새로운 클래스로 만드는것
상속을 받으면 기존 클래스에 있던 필드와 메소드를 물려받아 새로운 클래스를 만들수 있습니다.

15.오버라이드
상속받은 부모의 메소드를 재정의하여 사용하는것

16.추상메소드
자식클래스에서 상속받아서 오버라이딩해야만 사용할수있는 메소드

17.
추상 클래스와 인터페이스의 공통점
1. 스스로 객체 생성 불가능 (new 연산자 사용 불가)
2. 상속, 구현을 통해서만 사용가능 (메모리에 적개 가능)
3. 재정의를 통한 강제 구현이 목적

추상 클래스와 인터페이스의 다른점
1. 인터페이스는 유일하게 다중 상속이 가능
  Ex) interface Ia, interface Ib
  class Child implements Ia, Ib, Ic
2. 추상클래스는 단일상속이 원칙
3. 추상클래스(완성된 코드 + 미완성된 코드)
4. 인터페이스는 상수를 제외한 나머지는 미완성 코드(추상 자원)

18.다형성
다형성(polymorphism)이란 하나의 객체가 여러 가지 타입을 가질 수 있는 것을 의미합니다.